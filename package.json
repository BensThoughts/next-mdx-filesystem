{
  "name": "next-mdx-filesystem",
  "version": "0.1.0",
  "description": "A fully typed library to statically get .mdx files into next.js. next-mdx-filesystem will recursively read through a directory of your choosing and output a convenient tree or array data structure that includes .mdx/directory metadata and content.",
  "main": "index.ts",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "prepublish": "if [ -d './dist' ]; then rm -Rf ./dist; fi && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "next.js",
    "static",
    "mdx",
    "filesystem"
  ],
  "author": "Benjamin Blumenfeld-Jones",
  "license": "ISC",
  "dependencies": {
    "@corex/deepmerge": "^2.6.34",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.4",
    "@types/node": "^16.11.7"
  }
}
